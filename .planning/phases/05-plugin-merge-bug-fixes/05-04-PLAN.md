---
phase: 05-plugin-merge-bug-fixes
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - plugins/narrative-tool/manifest.json
  - plugins/narrative-tool/esbuild.config.mjs
  - plugins/narrative-tool/package.json
  - plugins/narrative-tool/src/main.js
  - plugins/narrative-tool/src/styles.css
  - plugins/narrative-tool/src/flow/navigation.js
  - tests/mocks/obsidian.js
  - tests/merge-smoke.test.js
  - tests/navigation.test.js
autonomous: true
requirements: [ENG-01, BUG-04, BUG-05, BUG-06, BUG-07]

must_haves:
  truths:
    - "All 10 commands are registered with the narrative-tool: prefix (D-08): export-current-dialogue, batch-export-all-dialogues, validate-references, create-character, create-location, create-item, create-quest, create-flow-canvas, create-flow-fragment, open-flow-canvas"
    - "Settings migrate automatically from legacy plugin data.json files on first load (D-06) — user does not re-enter Export Path"
    - "Flow Canvas / Flow Fragment commands create template-based canvases via createFlowCanvas / createFlowFragment (BUG-04)"
    - "From a Dialogue (.ncanvas) file the user can navigate back to its Flow canvas via command and right-click menu (BUG-05)"
    - "styles.css with data-nt-type rules (dialogue + entity) is injected at runtime (BUG-06)"
    - "File menu on .canvas files offers all 4 entity node types incl. Add quest node (BUG-07)"
  artifacts:
    - path: "plugins/narrative-tool/manifest.json"
      provides: "Merged plugin identity (D-05)"
      contains: '"id": "narrative-tool"'
    - path: "plugins/narrative-tool/src/main.js"
      provides: "NarrativeToolPlugin — all command/file-menu/CSS/migration wiring"
      exports: ["NarrativeToolPlugin"]
    - path: "plugins/narrative-tool/src/styles.css"
      provides: "Merged CSS (flow-tools data-nt-type rules + narrative-project status/settings styles)"
      contains: "data-nt-type"
    - path: "plugins/narrative-tool/esbuild.config.mjs"
      provides: "Single build config (D-10/D-11)"
      contains: "external"
    - path: "tests/merge-smoke.test.js"
      provides: "Proves all 10 narrative-tool: commands registered + migration runs on first load"
    - path: "tests/navigation.test.js"
      provides: "Proves findFlowCanvasForDialogue reverse-navigation lookup"
  key_links:
    - from: "plugins/narrative-tool/src/main.js"
      to: "plugins/narrative-tool/src/commands/export-current.js"
      via: "exportCurrentDialogue(this) command callback"
      pattern: "exportCurrentDialogue"
    - from: "plugins/narrative-tool/src/main.js"
      to: "plugins/narrative-tool/src/flow/navigation.js"
      via: "findFlowCanvasForDialogue for reverse navigation"
      pattern: "findFlowCanvasForDialogue"
    - from: "plugins/narrative-tool/src/main.js"
      to: "plugins/narrative-tool/src/styles.css"
      via: "require('./styles.css') + runtime injection"
      pattern: "require\\(\\./styles\\.css\\)"
---

<objective>
Create the merged plugin identity (manifest.json, esbuild.config.mjs, package.json), merge the two stylesheets into src/styles.css, add the reverse-navigation lookup to flow/navigation.js, and write the complete src/main.js — NarrativeToolPlugin — wiring every command, the settings migration, file-menu hooks (incl. BUG-07), template-based Flow Canvas/Fragment creation (BUG-04), reverse navigation (BUG-05), runtime CSS injection (BUG-06), the canvas node observer, and auto-export. Then add the merge smoke test and navigation tests.

Purpose: ENG-01 completion — the single plugin that replaces all three. This plan carries the four wiring bugs (BUG-04/05/06/07) and the D-06 settings migration.

Output: A loadable narrative-tool plugin skeleton (commands, settings tab, status bar, file menu, CSS, observer) with smoke-tested command inventory and migration behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md

Source of truth for the merged main.js behavior (read before writing):
@plugins/narrative-project/src/main.js (command bodies, status-bar state machine, setupAutoExport wiring)
@plugins/flow-tools/src/main.js (entity/flow command flows, file-menu hooks, CSS injection, observer)
@plugins/narrative-tool/src/commands/export-current.js (created in Plan 05-03 — import it)
@plugins/narrative-tool/src/flow/navigation.js (moved in Plan 05-02 — add findFlowCanvasForDialogue)
@plugins/flow-tools/src/styles.css (data-nt-type rules)
@plugins/narrative-project/styles.css (status bar + settings styles)

Restoration reference for flow-creation flows:
- `git show 4deaef8:plugins/flow-tools/src/main.js` — _createFlowCanvasFromCommand (template selection + _collectFlowCanvasParams), _createFlowFragmentFromCommand (+ _collectFlowFragmentParams), _pickNcanvasFile, _pickEntityFile, _getEntityFiles (4 types incl. quest folder 'Quests')

Test infrastructure (extend):
@tests/mocks/obsidian.js
@tests/batch-export.test.js (MockVault shape to reuse in merge-smoke.test.js)

<interfaces>
Command ID contract (D-08 — ALL unified to narrative-tool: prefix; D-09 documents the intentional hotkey break):
1. narrative-tool:export-current-dialogue
2. narrative-tool:batch-export-all-dialogues
3. narrative-tool:validate-references
4. narrative-tool:create-character
5. narrative-tool:create-location
6. narrative-tool:create-item
7. narrative-tool:create-quest
8. narrative-tool:create-flow-canvas
9. narrative-tool:create-flow-fragment
10. narrative-tool:open-flow-canvas

Module surface available (from Plans 05-01/02/03 — do NOT re-derive):
- commands/export-current.js: exportCurrentDialogue(plugin)
- commands/batch-export.js: exportAllDialogues(app, exportPath, exportScope, medEnabled)
- commands/auto-export.js: setupAutoExport(plugin, cb), teardownAutoExport(plugin)
- commands/reference-validator.js: validateReferences(app)
- ui/settings.js: DEFAULT_SETTINGS (exportPath 'Exports', medEnabled true, exportScope '/'), NarrativeToolSettingTab
- ui/status-bar.js: StatusBarManager (class narrative-tool-status)
- ui/notify.js: notify(message, type)
- ui/modals.js: FileSuggesterModal, StringSuggesterModal, FolderSuggestModal, promptForInput
- flow/entity-templates.js: createCharacterMd, createLocationMd, createQuestMd, createItemMd
- flow/canvas-templates.js: FLOW_TEMPLATES, FRAGMENT_TEMPLATES, createFlowCanvas, createFlowFragment
- flow/canvas-utils.js: generateNodeId, addNodeToCanvas, addDialogueNodeToCanvas
- flow/navigation.js: openDialogueFile, openFlowCanvas, openFileInSplit

Migration contract (D-06): on first load (loadData() === null), read .obsidian/plugins/{narrative-project,dialogue-export,flow-tools}/data.json via this.app.vault.adapter.read (guarded try/catch per file), Object.assign each parsed JSON over this.settings, saveData, notify. Guard: own data.json exists → skip (never re-runs over user edits — Pitfall 6).
</interfaces>
</context>
<tasks>

<task type="auto">
  <name>Task 1: Plugin identity — manifest.json, esbuild.config.mjs, package.json, merged styles.css</name>
  <files>
    plugins/narrative-tool/manifest.json
    plugins/narrative-tool/esbuild.config.mjs
    plugins/narrative-tool/package.json
    plugins/narrative-tool/src/styles.css
  </files>
  <read_first>
    plugins/dialogue-export/esbuild.config.mjs (structure to copy)
    plugins/dialogue-export/package.json (structure to copy)
    plugins/dialogue-export/manifest.json (field layout)
    plugins/flow-tools/src/styles.css (data-nt-type rules — the BUG-06 CSS)
    plugins/narrative-project/styles.css (status bar states + settings rules)
  </read_first>
  <action>
    Create four files:

    1. plugins/narrative-tool/manifest.json (D-05): JSON with id "narrative-tool", name "Narrative Tool", version "1.0.0", minAppVersion "1.5.0", description "Narrative toolchain: Flow → Dialogue editing and Godot Dialogue Manager export", author "Narrative Toolchain", isDesktopOnly true.

    2. plugins/narrative-tool/esbuild.config.mjs (D-10/D-11): copy the structure of plugins/dialogue-export/esbuild.config.mjs (esbuild.context with rebuild, --watch flag, banner from package.json). Differences: entryPoints ['src/main.js'] (single entry per D-11), outfile 'main.js', external ['obsidian', 'electron'] (D-11 — drop the @codemirror/* and @lezer/* externals), add `loader: { '.css': 'text' }` (required so main.js can require('./styles.css') and inject it at runtime per BUG-06 / ARCHITECTURE.md). Keep platform 'node', target 'node18', format 'cjs', minify for build, treeShaking true.

    3. plugins/narrative-tool/package.json (D-10): name "narrative-tool", version "1.0.0", private true, scripts { "build": "node esbuild.config.mjs", "dev": "node esbuild.config.mjs --watch" }, devDependencies { "esbuild": "^0.28.1" } (matches the version already vendored in the three legacy plugin lockfiles — do not bump).

    4. plugins/narrative-tool/src/styles.css — merged stylesheet. Read both source files fully, then produce one file with:
       a. The flow-tools/src/styles.css content verbatim (the data-nt-type rules for dialogue/entity/character/location/quest/item — these are the BUG-06 rules that were never actually loaded because the file lived in src/ and was never injected).
       b. The narrative-project/styles.css content with the prefix rename: every occurrence of 'narrative-project-status' → 'narrative-tool-status' (matches the status-bar.js rename from Plan 05-03). The nt-status-* state classes and @keyframes nt-spin stay as-is.
       c. A short header comment noting the merge (source files + date).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('./plugins/narrative-tool/manifest.json','utf8')); const p=JSON.parse(fs.readFileSync('./plugins/narrative-tool/package.json','utf8')); const css=fs.readFileSync('./plugins/narrative-tool/src/styles.css','utf8'); console.log(m.id, m.name, p.name, p.devDependencies.esbuild, css.includes('data-nt-type'), css.includes('narrative-tool-status'), !css.includes('narrative-project-status'))"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json parses with id 'narrative-tool' and name 'Narrative Tool'; package.json parses with name 'narrative-tool' and devDependencies.esbuild '^0.28.1'
    - esbuild.config.mjs contains `external` with 'obsidian' and 'electron' (D-11) and `loader` with '.css' (verify command output prints all expected values: m.id, m.name, p.name, esbuild version, css flags)
    - src/styles.css contains 'data-nt-type' rules and 'narrative-tool-status'; contains zero 'narrative-project-status' occurrences
  </acceptance_criteria>
  <done>Plugin identity files created per D-05/D-10/D-11; merged styles.css carries BUG-06 rules with renamed status class</done>
</task>
<task type="auto">
  <name>Task 2: Add findFlowCanvasForDialogue + write the full src/main.js (NarrativeToolPlugin)</name>
  <files>
    plugins/narrative-tool/src/flow/navigation.js
    plugins/narrative-tool/src/main.js
  </files>
  <read_first>
    plugins/narrative-tool/src/flow/navigation.js (current 82-line module — append the new function, do not rewrite existing ones)
    plugins/narrative-project/src/main.js (batchExportAllDialogues / runReferenceValidation / status-bar state machine / setupAutoExport callback)
    plugins/flow-tools/src/main.js (entity command config + _createEntityFromCommand, file-menu hooks, _injectCanvasStyles, _setupCanvasNodeTypeObserver, _annotateAllCanvasViews, _createDialogueNodeOnCanvas, _addFileNodeToCanvasFile, _openLinkedDialogueFromCanvas, _getEntityFiles)
    Run `git show 4deaef8:plugins/flow-tools/src/main.js` for _createFlowCanvasFromCommand (template selection + _collectFlowCanvasParams), _createFlowFragmentFromCommand (+ _collectFlowFragmentParams), _pickNcanvasFile, _pickEntityFile
    plugins/narrative-tool/src/commands/export-current.js (Plan 05-03 output — exportCurrentDialogue(plugin))
    .planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md (D-06, D-08, D-14)
  </read_first>
  <action>
    Step 1 — plugins/narrative-tool/src/flow/navigation.js: append and export a new function:
    `async function findFlowCanvasForDialogue(app, ncanvasPath)` → returns Promise<TFile[]>.
    Logic: const results = []; for each file in app.vault.getFiles() with extension 'canvas': try read + JSON.parse; if (json.nodes || []).some(n => n.type === 'file' && n.file === ncanvasPath) → results.push(canvasFile); catch → skip unparseable canvas silently. Return results. Add to module.exports alongside openFlowCanvas.

    Step 2 — create plugins/narrative-tool/src/main.js (module.exports = class NarrativeToolPlugin extends Plugin). Structure:

    a. Imports (single require('obsidian') line): { Plugin, TFile, normalizePath } — plus whatever the copied flows need (Notice comes via ui/notify only). Then:
    - const { DEFAULT_SETTINGS, NarrativeToolSettingTab } = require('./ui/settings');
    - const { StatusBarManager } = require('./ui/status-bar');
    - const { notify } = require('./ui/notify');
    - const { FileSuggesterModal, StringSuggesterModal, promptForInput } = require('./ui/modals');
    - const { exportAllDialogues } = require('./commands/batch-export');
    - const { setupAutoExport, teardownAutoExport } = require('./commands/auto-export');
    - const { validateReferences } = require('./commands/reference-validator');
    - const { exportCurrentDialogue } = require('./commands/export-current');
    - const { createCharacterMd, createLocationMd, createQuestMd, createItemMd } = require('./flow/entity-templates');
    - const { FLOW_TEMPLATES, FRAGMENT_TEMPLATES, createFlowCanvas, createFlowFragment } = require('./flow/canvas-templates');
    - const { generateNodeId, addNodeToCanvas } = require('./flow/canvas-utils');
    - const { openDialogueFile, openFlowCanvas, openFileInSplit, findFlowCanvasForDialogue } = require('./flow/navigation');
    - const styles = require('./styles.css');

    b. Helper functions at module scope (copied from flow-tools main.js): slugify (verbatim). promptForInput is imported — do NOT redefine.

    c. onload() — order:
    1. `const saved = await this.loadData();` then `this.settings = Object.assign({}, DEFAULT_SETTINGS, saved);` then migration (D-06): if (!saved) — for each id of ['narrative-project', 'dialogue-export', 'flow-tools']: try { const raw = await this.app.vault.adapter.read('.obsidian/plugins/' + id + '/data.json'); const json = JSON.parse(raw); Object.assign(this.settings, json); } catch (e) { /* file absent or unreadable — skip */ } — then await this.saveData(this.settings) and notify('已迁移旧插件设置', 'info') (Chinese — new message per D-14). Guard comment: own data.json exists → migration skipped, never re-runs over user edits.
    2. this.addSettingTab(new NarrativeToolSettingTab(this.app, this));
    3. this.statusBar = new StatusBarManager(this);
    4. setupAutoExport(this, callback) — same status-bar state machine as narrative-project main.js (success/failure/pending 5s auto-revert).
    5. Register 10 commands, IDs per the interfaces contract (all with narrative-tool: prefix, D-08):
       - narrative-tool:export-current-dialogue, name 'Export current dialogue' → exportCurrentDialogue(this)
       - narrative-tool:batch-export-all-dialogues, name 'Batch Export All Dialogues' → this.batchExportAllDialogues()
       - narrative-tool:validate-references, name 'Validate Flow→Dialogue references' → this.runReferenceValidation()
       - 4 entity commands via an entities array (id/name/modalTitle/templateFn/defaultFolder/entityType): create-character ('Create Character', createCharacterMd, 'Characters', 'character'), create-location ('Create Location', createLocationMd, 'Locations', 'location'), create-item ('Create Item', createItemMd, 'Items', 'item'), create-quest ('Create Quest', createQuestMd, 'Quests', 'quest') — each → this._createEntityFromCommand(cmd)
       - narrative-tool:create-flow-canvas, name 'Create Flow Canvas' → this._createFlowCanvasFromCommand() (BUG-04)
       - narrative-tool:create-flow-fragment, name 'Create Flow Fragment' → this._createFlowFragmentFromCommand() (BUG-04)
       - narrative-tool:open-flow-canvas, name 'Open Flow Canvas' → this._openFlowCanvasFromCommand() (BUG-05)
    6. this._registerFileMenuHooks();
    7. this._injectCanvasStyles(); (BUG-06)
    8. this._setupCanvasNodeTypeObserver();

    d. Method bodies (copied from the three legacy main.js files with these exact changes):
    - batchExportAllDialogues() / runReferenceValidation(): verbatim from narrative-project main.js, except every new Notice(...) call → notify(...) (D-14), same strings.
    - _createEntityFromCommand(cmd): verbatim from flow-tools main.js (slugify id, per-type defaults, sanitize + startsWith folder path-traversal check, duplicate check, vault.create, openLinkText) with: quest defaults case ('quest': questType '', prerequisites [], stages [], giverCharacterId '', involvedLocationIds [], notes ''), all Notices → notify.
    - _createFlowCanvasFromCommand(): restore the 4deaef8 version (template selection via StringSuggesterModal over [Chapter, Quest, World Event] → _collectFlowCanvasParams → createFlowCanvas(templateType, params) → Flows/<slug>.canvas with path-traversal checks → vault.create → openLinkText) — this wires BUG-04 (current main.js builds a minimal inline canvas instead; replace that).
    - _collectFlowCanvasParams(templateType): verbatim from 4deaef8 (Chapter: title/entryScene/npcs/locations via _pickNcanvasFile + promptForInput; Quest: questName/giverChar via _pickEntityFile('character')/stages/reward; World Event: eventName/trigger/affectedLocs via _pickEntityFile('location')/outcome).
    - _createFlowFragmentFromCommand(): restore the 4deaef8 version (Quest Detail / Scene Breakdown selection → _collectFlowFragmentParams → createFlowFragment → Flows/<slug>.canvas) — BUG-04.
    - _collectFlowFragmentParams(templateType): verbatim from 4deaef8.
    - _pickNcanvasFile(title) / _pickEntityFile(entityType, title): verbatim from 4deaef8 (use imported FileSuggesterModal).
    - _createDialogueNodeOnCanvas(canvasFile): verbatim from current flow-tools main.js (minimal .ncanvas Entry→Dialog JSON + file node add).
    - _addFileNodeToCanvasFile(canvasFile, entityFiles, entityType): verbatim from current flow-tools main.js with ENTITY_COLORS extended: { character: '4', location: '2', item: '1', quest: '3' } (quest purple — Claude's discretion, documented).
    - _openLinkedDialogueFromCanvas(canvasFile): verbatim (uses openDialogueFile + FileSuggesterModal).
    - _getEntityFiles(entityType): folderMap { character: 'Characters', location: 'Locations', item: 'Items', quest: 'Quests' }.
    - _openFlowCanvasFromCommand() (BUG-05): activeFile = workspace.getActiveFile(); if (!activeFile || activeFile.extension !== 'ncanvas') → notify('Open a .ncanvas file first'); else → this._openFlowCanvasForFile(activeFile).
    - _openFlowCanvasForFile(file) (BUG-05): const canvases = await findFlowCanvasForDialogue(this.app, file.path); 0 → notify('No Flow canvas references this dialogue'); 1 → openFlowCanvas(this.app, canvases[0].path); >1 → FileSuggesterModal pick → openFlowCanvas(this.app, chosen.path).
    - _registerFileMenuHooks(): this.registerEvent(this.app.workspace.on('file-menu', (menu, file) => {...})); if (file instanceof TFile && file.extension === 'canvas') → items: 'Add dialogue node' (message-square icon, → _createDialogueNodeOnCanvas(file)), 'Add character node' (user, → _addFileNodeToCanvasFile(file, this._getEntityFiles('character'), 'character')), 'Add location node' (map-pin, location), 'Add item node' (package, item), 'Add quest node' (target, quest) — BUG-07 completes all 4 entity types — then separator, 'Open linked dialogue' (external-link, → _openLinkedDialogueFromCanvas(file)); else if (file instanceof TFile && file.extension === 'ncanvas') → item 'Open flow canvas' (external-link, → _openFlowCanvasForFile(file)) — BUG-05 right-click entry. All existing menu item titles kept verbatim.
    - _injectCanvasStyles() (BUG-06): remove existing document.getElementById('narrative-tool-styles') if present; const styleEl = document.createElement('style'); styleEl.id = 'narrative-tool-styles'; styleEl.textContent = styles; document.head.appendChild(styleEl).
    - _setupCanvasNodeTypeObserver(): keep the current hybrid (setInterval 1s poll + layout-change debounced 200ms + initial scans 500/1500/3000ms) but restore entity detection in _annotateAllCanvasViews: for each .canvas-node without data-nt-type, read the label element text: endsWith('.ncanvas') → setAttribute('data-nt-type', 'dialogue'); endsWith('.md') → setAttribute('data-nt-type', 'entity'); also keep the current attribute/iframe/child-element .ncanvas fallback checks (do not remove working dialogue detection — only ADD the .md label check).
    - onunload(): teardownAutoExport(this); clearTimeout(this._autoExportTimeout); if (this.statusBar) this.statusBar.destroy(); clearInterval(this._observerInterval); document.getElementById('narrative-tool-styles')?.remove();

    e. Every user-facing message goes through notify() (D-14). Existing English strings verbatim; genuinely new messages in Chinese (per D-14: notify wrapper is the Chinese-ready consolidation point; the full sweep is Phase 8).
  </action>
  <verify>
    <automated>node -e "const Module=require('node:module'),path=require('node:path'),orig=Module._resolveFilename; Module._resolveFilename=function(r,p,i,o){return (r==='obsidian'||r==='obsidian/')?path.join('tests','mocks','obsidian.js'):orig.call(this,r,p,i,o);}; Module._extensions['.css']=function(mod,filename){mod._compile('module.exports = 1;',filename);}; const m=require('./plugins/narrative-tool/src/main.js'); const n=require('./plugins/narrative-tool/src/flow/navigation'); console.log(typeof m, typeof n.findFlowCanvasForDialogue)"</automated>
  </verify>
  <acceptance_criteria>
    - plugins/narrative-tool/src/main.js exports NarrativeToolPlugin class; loads without syntax errors
    - `grep -c "narrative-tool:" plugins/narrative-tool/src/main.js` ≥ 10 (all command IDs present with prefix, D-08)
    - `grep -c "createFlowCanvas\|createFlowFragment" plugins/narrative-tool/src/main.js` ≥ 2 (BUG-04 wired)
    - `grep -c "findFlowCanvasForDialogue" plugins/narrative-tool/src/main.js plugins/narrative-tool/src/flow/navigation.js` ≥ 2 (BUG-05 wired: command + file-menu + finder)
    - `grep -c "Add quest node" plugins/narrative-tool/src/main.js` ≥ 1 and `grep -c "'quest'" plugins/narrative-tool/src/main.js` ≥ 1 (BUG-07)
    - `grep -c "data-nt-type" plugins/narrative-tool/src/main.js` ≥ 2 (observer sets dialogue + entity — BUG-06 annotation)
    - `grep -c "new Notice(" plugins/narrative-tool/src/main.js` returns 0 (all messages via notify per D-14)
  </acceptance_criteria>
  <done>main.js registers all 10 narrative-tool: commands, migration (D-06), file-menu (BUG-07), flow templates (BUG-04), reverse nav (BUG-05), CSS injection + entity annotation (BUG-06)</done>
</task>
<task type="auto">
  <name>Task 3: Extend the obsidian mock, add merge-smoke.test.js and navigation.test.js</name>
  <files>
    tests/mocks/obsidian.js
    tests/merge-smoke.test.js
    tests/navigation.test.js
  </files>
  <read_first>
    tests/mocks/obsidian.js (current stubs: PluginSettingTab, Setting, Plugin, TFolder, SuggestModal; exports at line 167)
    tests/batch-export.test.js (Module._resolveFilename interception + MockVault shape to reuse)
    tests/auto-export.test.js (mock interception pattern)
    plugins/narrative-tool/src/main.js (created in Task 2 — the class under test)
  </read_first>
  <action>
    Step 1 — extend tests/mocks/obsidian.js:
    - Add `class Notice { constructor(message) { this.message = message; Notice._last = message; Notice._all = Notice._all || []; Notice._all.push(message); this.noticeEl = { addClass(cls) { this._class = cls; } }; } }` (static capture so tests can assert messages).
    - Add `class Modal { constructor(app) { this.app = app; this.titleEl = { setText() {} }; this.contentEl = { createEl() { return { style: {} }; } }; } open() {} close() {} }`.
    - Add `class TFile { constructor(path) { this.path = path; this.basename = path.split('/').pop().replace(/\.[^.]+$/, ''); this.extension = path.split('.').pop(); } }`.
    - Add `function normalizePath(p) { return p; }` (identity).
    - Extend module.exports: add Notice, Modal, TFile, normalizePath to the existing export list.

    Step 2 — create tests/merge-smoke.test.js (node:test + node:assert):
    - Setup: Module._resolveFilename interception for 'obsidian' → tests/mocks/obsidian.js (copy the pattern from batch-export.test.js lines 21-29). ALSO register a CSS loader so require('./styles.css') inside main.js does not throw: `Module._extensions['.css'] = function (mod, filename) { mod._compile('module.exports = /* css */ "";', filename); };`
    - MockVault: copy the class shape from batch-export.test.js (files map, getFiles/getAbstractFileByPath/create/modify/createFolder/read) and ADD: `on(event, cb) { return { event, cb }; }` (for setupAutoExport's vault.on('modify')) and `adapter: { read: async (p) => { if (p === '.obsidian/plugins/narrative-project/data.json') return JSON.stringify({ exportPath: 'Legacy/Path', medEnabled: false }); throw new Error('ENOENT'); } }`.
    - MockApp: { vault: mockVault, workspace: { getActiveFile: () => null, activeLeaf: null, on: () => ({}) }, plugins: {} }.
    - Test 1 — command inventory: const NarrativeToolPlugin = require('../plugins/narrative-tool/src/main.js'); const plugin = new NarrativeToolPlugin(mockApp, { version: '1.0.0' }); await plugin.onload(); assert plugin._commands.length === 10; assert every command id starts with 'narrative-tool:'; assert the id set equals exactly: export-current-dialogue, batch-export-all-dialogues, validate-references, create-character, create-location, create-item, create-quest, create-flow-canvas, create-flow-fragment, open-flow-canvas (each prefixed).
    - Test 2 — first-load migration (D-06): with loadData() returning null (override plugin.loadData = async () => null BEFORE onload), after onload assert plugin.settings.exportPath === 'Legacy/Path' and plugin.settings.medEnabled === false (merged over DEFAULT_SETTINGS from the mock adapter data); assert saveData was called with the migrated settings (wrap plugin.saveData with a capture). Also assert the migration Notice was shown (Notice._all contains the migration message).
    - Test 3 — migration skipped when own data.json exists: plugin.loadData = async () => ({ exportPath: 'Own/Path' }); mock adapter.read throws for ALL paths; after onload assert plugin.settings.exportPath === 'Own/Path' and adapter.read was never invoked (track calls on the adapter object).
    - Test 4 — settings tab + status bar: assert plugin._settingTab is an instance of NarrativeToolSettingTab (import from '../plugins/narrative-tool/src/ui/settings') and plugin.statusBar exists.
    - Note: onload also calls setupAutoExport (vault.on) and workspace.on('file-menu') — both mocked to return {} (registerEvent no-op). The statusBar uses addStatusBarItem from the mock Plugin.

    Step 3 — create tests/navigation.test.js (node:test + node:assert):
    - Setup FIRST: Module._resolveFilename interception for 'obsidian' → tests/mocks/obsidian.js (copy the pattern from merge-smoke.test.js Step 2 — flow/navigation.js has `const { Notice } = require('obsidian')` at module top, so importing it without the mock throws Cannot find module 'obsidian').
    - MockVault (same shape, plus a helper addCanvas(path, nodes) that stores JSON content).
    - Test 1 — finds the flow canvas: vault with 'Flows/ch1.canvas' containing a file node { type: 'file', file: 'Dialogues/Innkeeper.ncanvas' } and 'Flows/ch2.canvas' without it; const found = await findFlowCanvasForDialogue(app, 'Dialogues/Innkeeper.ncanvas'); assert found.length === 1 and found[0].path === 'Flows/ch1.canvas'.
    - Test 2 — multiple references: two canvases both referencing the file → length 2.
    - Test 3 — none: no canvas references → [].
    - Test 4 — unparseable canvas skipped: a canvas with invalid JSON content → not in results, no throw.
  </action>
  <verify>
    <automated>node --test tests/merge-smoke.test.js tests/navigation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Notice" tests/mocks/obsidian.js` ≥ 1; `grep -c "normalizePath" tests/mocks/obsidian.js` ≥ 1
    - tests/merge-smoke.test.js passes: 10 commands with narrative-tool: prefix asserted; migration-on-first-load and migration-skip cases asserted
    - tests/navigation.test.js passes: found/multiple/none/unparseable cases
  </acceptance_criteria>
  <done>Mock extended; merge smoke test proves command inventory + migration; navigation test proves reverse lookup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.js → vault adapter | Migration reads .obsidian/plugins/*/data.json (user's own plugin config) on first load |
| main.js → DOM | Runtime CSS injection + canvas node annotation (data-nt-type) |
| main.js → commands/flow/ui modules | All wiring passes through the layer modules built in Plans 05-01..03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | settings migration (D-06) | mitigate | Guarded by loadData() === null (own data.json exists → skip, never re-runs over user edits); per-file try/catch (absent/unreadable legacy files skipped); merge is Object.assign over DEFAULT_SETTINGS — only known keys win; smoke test asserts both branches |
| T-05-10 | Tampering | entity/flow file creation path traversal | mitigate | Preserve flow-tools' sanitize + `filePath.startsWith(folder + '/')` checks in _createEntityFromCommand / _createFlowCanvasFromCommand (Pitfall 6 security checklist — the merge must not drop them); they are restored verbatim per 4deaef8 |
| T-05-11 | Spoofing | runtime CSS injection (BUG-06) | accept | styles content is the plugin's own committed asset (esbuild text loader); no user input reaches the injected string; idempotent removal on unload |
| T-05-12 | Tampering | nc-bridge constraint | accept | main.js never references window.NarrativeCanvasHost (D-13 — zero bridge calls); nc-bridge.js from Plan 05-03 is the only allowed touchpoint |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; esbuild install is Plan 05-05 (already vendored in-repo, version-aligned) |
</threat_model>

<verification>
- `node --test tests/merge-smoke.test.js tests/navigation.test.js` — all pass
- `node -e "require('./plugins/narrative-tool/src/main.js')"` — module loads without syntax errors
- Full regression spot-check: `node --test tests/constants.test.js tests/entity-templates.test.js tests/batch-export.test.js tests/settings.test.js tests/status-bar.test.js` — all pass (layers from Plans 01-03 remain green)
- `git diff --stat tests/golden` unchanged (byte-identical contract)
- Grep gate: `grep -c "new Notice(" plugins/narrative-tool/src/main.js` returns 0 (all messages via notify per D-14)
</verification>

<success_criteria>
- [ ] manifest.json / esbuild.config.mjs / package.json / styles.css exist with the specified identity (D-05/D-10/D-11)
- [ ] main.js registers exactly 10 commands with narrative-tool: prefix (smoke-tested, D-08)
- [ ] D-06 migration implemented with skip-guard (smoke-tested both branches)
- [ ] BUG-04 wired: create-flow-canvas / create-flow-fragment use createFlowCanvas / createFlowFragment template flows
- [ ] BUG-05 wired: open-flow-canvas command + .ncanvas file-menu item via findFlowCanvasForDialogue (navigation tests pass)
- [ ] BUG-06 wired: styles.css injected at runtime with id narrative-tool-styles; entity .md annotation restored
- [ ] BUG-07 wired: Add quest node (+ item) file-menu items complete the 4 entity types
- [ ] No golden .dialogue modified
</success_criteria>

<output>
Create `.planning/phases/05-plugin-merge-bug-fixes/05-04-SUMMARY.md` when done
</output>
