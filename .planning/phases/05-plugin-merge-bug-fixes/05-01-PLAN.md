---
phase: 05-plugin-merge-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/narrative-tool/src/engine/export-engine.js
  - plugins/narrative-tool/src/engine/gd-format.js
  - plugins/narrative-tool/src/engine/med-format.js
  - plugins/narrative-tool/src/engine/gd-constants.js
  - plugins/narrative-tool/src/engine/med-constants.js
  - tests/export-base.test.js
  - tests/export-med.test.js
  - tests/export-plugin.test.js
  - tests/constants.test.js
  - tests/engine-purity.test.js
autonomous: true
requirements: [ENG-01]

must_haves:
  truths:
    - "Engine modules (export-engine, gd-format, med-format) importable from plugins/narrative-tool/src/engine/ with zero obsidian imports"
    - "gd-constants.js and med-constants.js live inside engine/ per D-03 (merged into engine/, not repo-root shared/)"
    - "Engine test files import from the new engine/ location and pass"
  artifacts:
    - path: "plugins/narrative-tool/src/engine/export-engine.js"
      provides: "Moved export engine (verbatim, git-mv preserves history + uncommitted changes)"
      exports: ["exportEngine", "topologicalSort", "resolveCharacter"]
    - path: "plugins/narrative-tool/src/engine/gd-format.js"
      provides: "Moved gd formatter with local constants import"
      contains: "require('./gd-constants')"
    - path: "plugins/narrative-tool/src/engine/med-format.js"
      provides: "Moved med formatter with local constants imports"
      contains: "require('./med-constants')"
    - path: "plugins/narrative-tool/src/engine/gd-constants.js"
      provides: "TOKENS constants relocated per D-03"
    - path: "plugins/narrative-tool/src/engine/med-constants.js"
      provides: "MED_TOKENS constants relocated per D-03"
    - path: "tests/engine-purity.test.js"
      provides: "Guard that engine/ never imports obsidian or touches window/document"
  key_links:
    - from: "plugins/narrative-tool/src/engine/gd-format.js"
      to: "plugins/narrative-tool/src/engine/gd-constants.js"
      via: "require('./gd-constants')"
      pattern: "require\\(\\./gd-constants\\)"
    - from: "tests/export-base.test.js"
      to: "plugins/narrative-tool/src/engine/export-engine"
      via: "require path update"
      pattern: "require\\(\\.\\./plugins/narrative-tool/src/engine/export-engine\\)"
---

<objective>
Move the pure export engine (export-engine.js, gd-format.js, med-format.js) and the shared constants (gd-constants.js, med-constants.js) into the new merged plugin's engine/ layer, and re-point all engine test imports.

Purpose: Establish the final engine/ home first (ENG-01). Per D-03, the shared constants merge into engine/ — they are export-engine dependencies. Per D-02, engine/ is the pure layer: zero `obsidian` imports, testable via plain `node --test`. The merge is 100% mechanical: files move via `git mv` (preserves git history AND uncommitted working-tree changes — gd-format.js currently has in-flight edits), internal require paths change, behavior is byte-identical (9 goldens must not change).

Output: plugins/narrative-tool/src/engine/ populated; tests import from the new location; a purity guard test enforces the layer boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md

Source files (current pre-merge state — read before moving):
@plugins/dialogue-export/src/export-engine.js
@plugins/dialogue-export/src/gd-format.js
@plugins/dialogue-export/src/med-format.js
@shared/gd-constants.js
@shared/med-constants.js

Test files whose require paths change:
@tests/export-base.test.js
@tests/export-med.test.js
@tests/export-plugin.test.js
@tests/constants.test.js

<interfaces>
From plugins/dialogue-export/src/export-engine.js (module.exports):
- exportEngine(ncanvasJson, config) -> string
- topologicalSort(nodes, links, startId) -> Array
- resolveCharacter(node, characters) -> string|null

From plugins/dialogue-export/src/gd-format.js (module.exports):
- formatNode(node, ctx), slugifyCueName(name, fallbackId), formatChoiceNode(node, ctx), FORMATTERS
- Internal require line 9: const { TOKENS } = require('../../../shared/gd-constants');

From plugins/dialogue-export/src/med-format.js (module.exports):
- formatMedHeader(ncanvas), detectMedState(ncanvas), formatMedNode(node, ctx), formatMutationsForEffects(effects, depth)
- Internal require lines 19-20: require('../../../shared/med-constants'), require('../../../shared/gd-constants')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move engine modules and shared constants into engine/ (git mv + import fix)</name>
  <files>
    plugins/narrative-tool/src/engine/export-engine.js
    plugins/narrative-tool/src/engine/gd-format.js
    plugins/narrative-tool/src/engine/med-format.js
    plugins/narrative-tool/src/engine/gd-constants.js
    plugins/narrative-tool/src/engine/med-constants.js
  </files>
  <read_first>
    plugins/dialogue-export/src/export-engine.js
    plugins/dialogue-export/src/gd-format.js (note: has uncommitted working-tree changes — must be preserved)
    plugins/dialogue-export/src/med-format.js
    shared/gd-constants.js
    shared/med-constants.js
    .planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Create directory plugins/narrative-tool/src/engine/ (with Write tool: create a placeholder .gitkeep or the first moved file). Then move files with `git mv` — NOT cp+delete (git mv preserves history and carries uncommitted working-tree changes; do not manually copy):

    git mv plugins/dialogue-export/src/export-engine.js plugins/narrative-tool/src/engine/export-engine.js
    git mv plugins/dialogue-export/src/gd-format.js plugins/narrative-tool/src/engine/gd-format.js
    git mv plugins/dialogue-export/src/med-format.js plugins/narrative-tool/src/engine/med-format.js
    git mv shared/gd-constants.js plugins/narrative-tool/src/engine/gd-constants.js
    git mv shared/med-constants.js plugins/narrative-tool/src/engine/med-constants.js

    After the moves, edit ONLY the require paths in the moved files (no other content changes — behavior must stay byte-identical):

    1. plugins/narrative-tool/src/engine/gd-format.js: change line 9 from require('../../../shared/gd-constants') to require('./gd-constants'). Keep the destructure `const { TOKENS } = ...` unchanged.
    2. plugins/narrative-tool/src/engine/med-format.js: change line 19 require('../../../shared/med-constants') to require('./med-constants') and line 20 require('../../../shared/gd-constants') to require('./gd-constants'). Keep destructures unchanged.

    Do NOT touch export-engine.js content (it imports './gd-format' and './med-format' — sibling paths survive the move unchanged).

    Note: shared/schema/ must NOT be moved in this plan — it stays at shared/schema/ until Plan 05-02 (its consumers, flow/entity-templates.js and tests/schema.test.js, are not touched here).

    Git status must show only renames (R) for these files; verify with `git status --short plugins/narrative-tool shared` that no file content beyond the two require edits differs.
  </action>
  <verify>
    <automated>node -e "const e=require('./plugins/narrative-tool/src/engine/export-engine'); const g=require('./plugins/narrative-tool/src/engine/gd-format'); const m=require('./plugins/narrative-tool/src/engine/med-format'); const c=require('./plugins/narrative-tool/src/engine/gd-constants'); console.log(typeof e.exportEngine, typeof g.formatNode, typeof m.formatMedHeader, typeof c.TOKENS)"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short plugins/narrative-tool/src/engine shared` shows the 5 files as renames (R) from their old locations; shared/ contains only schema/ (gd-constants.js and med-constants.js gone from shared/)
    - `grep -v '^#' plugins/narrative-tool/src/engine/gd-format.js | grep -c "require('./gd-constants')"` returns 1 and `grep -c "shared/" plugins/narrative-tool/src/engine/gd-format.js` returns 0
    - `grep -v '^#' plugins/narrative-tool/src/engine/med-format.js | grep -c "require('./med-constants')\|require('./gd-constants')"` returns 2 and `grep -c "shared/" plugins/narrative-tool/src/engine/med-format.js` returns 0
    - The smoke require command above prints function types for all four modules
  </acceptance_criteria>
  <done>All 5 files live in plugins/narrative-tool/src/engine/; gd-format.js and med-format.js import constants via './...' relative paths; export-engine.js untouched; shared/ has only schema/ remaining</done>
</task>

<task type="auto">
  <name>Task 2: Re-point engine test imports to the new engine/ location</name>
  <files>
    tests/export-base.test.js
    tests/export-med.test.js
    tests/export-plugin.test.js
    tests/constants.test.js
  </files>
  <read_first>
    tests/export-base.test.js (require at line 8, plus lines 57/69/81/90/97/103)
    tests/export-med.test.js (lines 6, 9, 11)
    tests/export-plugin.test.js (line 6)
    tests/constants.test.js (lines 18, 107, 183, 184)
  </read_first>
  <action>
    Update every require path that points into the old plugin/shared locations to the new engine/ location. Exact replacements (old string → new string, applied wherever they appear in these 4 files):

    1. tests/export-base.test.js: `'../plugins/dialogue-export/src/export-engine'` → `'../plugins/narrative-tool/src/engine/export-engine'` (all occurrences — lines 8, 57, 69, 81, 90, 97, 103)
    2. tests/export-med.test.js: `'../shared/med-constants'` → `'../plugins/narrative-tool/src/engine/med-constants'` (line 6); `'../plugins/dialogue-export/src/med-format'` → `'../plugins/narrative-tool/src/engine/med-format'` (line 9); `'../plugins/dialogue-export/src/export-engine'` → `'../plugins/narrative-tool/src/engine/export-engine'` (line 11)
    3. tests/export-plugin.test.js: `'../plugins/dialogue-export/src/export-engine'` → `'../plugins/narrative-tool/src/engine/export-engine'` (line 6)
    4. tests/constants.test.js: `'../shared/gd-constants.js'` → `'../plugins/narrative-tool/src/engine/gd-constants.js'` (lines 18, 183); `'../shared/med-constants.js'` → `'../plugins/narrative-tool/src/engine/med-constants.js'` (lines 107, 184)

    Do NOT edit any test assertions — only require paths. Do NOT touch tests/schema.test.js (Plan 05-02), tests/entity-templates.test.js (Plan 05-02), or tests/canvas-templates.test.js (Plan 05-02). tests/export.test.js (the aggregator) needs no changes — its requires are relative within tests/.
  </action>
  <verify>
    <automated>node --test tests/export-base.test.js tests/export-med.test.js tests/export-plugin.test.js tests/constants.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "plugins/dialogue-export\|'../shared/" tests/export-base.test.js tests/export-med.test.js tests/export-plugin.test.js tests/constants.test.js` returns zero matches
    - The 4 engine test files pass: `node --test` output shows pass count ≥ 130 with fail 0 (engine suite currently ~130+ tests)
  </acceptance_criteria>
  <done>All engine tests import from plugins/narrative-tool/src/engine/ and pass</done>
</task>

<task type="auto">
  <name>Task 3: Add engine purity guard test (layer boundary enforcement)</name>
  <files>tests/engine-purity.test.js</files>
  <read_first>
    plugins/narrative-tool/src/engine/gd-format.js (import structure after Task 1)
    tests/constants.test.js (node:test + readFileSync pattern to follow)
  </read_first>
  <action>
    Create tests/engine-purity.test.js using the node:test + node:assert pattern (2-space indent, double quotes, per CONVENTIONS.md). It scans every .js file under plugins/narrative-tool/src/engine/ (including engine/schema/ if present) via readFileSync and asserts the file content does NOT contain:

    1. `require('obsidian')` or `require("obsidian")` — pattern: /require\(\s*['"]obsidian['"]\s*\)/
    2. `window.` — pattern: /window\s*\./
    3. `document.` — pattern: /document\s*\./

    Implement as: walk the engine dir with readdirSync (recursive through subdirectories, filter .js), for each file read content and collect violations into an array of {file, matches}; assert.deepStrictEqual(violations, []) with a descriptive message listing each violation. Include a positive control test: a temp string containing "require('obsidian')" is detected by the same regex (guards against a broken matcher). Exclude nothing — engine/ must stay pure per ARCHITECTURE.md layer rules.

    Also add a test asserting that plugins/narrative-tool/src/engine/gd-constants.js exists and exports TOKENS (sanity that the D-03 relocation landed — export name TOKENS from shared/gd-constants.js is preserved).
  </action>
  <verify>
    <automated>node --test tests/engine-purity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/engine-purity.test.js exists and passes (fail 0)
    - The scan covers at least 5 files (export-engine, gd-format, med-format, gd-constants, med-constants): assert scanned file count ≥ 5 in the test
    - Positive control passes: the matcher detects 'require("obsidian")' in a synthetic string
  </acceptance_criteria>
  <done>Purity guard test exists and enforces the engine/ boundary: no obsidian require, no window./document. access</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine/ modules → shared constants | Pure data flow, no I/O; constants are static frozen objects |
| tests → engine modules | Node test runner imports engine directly (no Obsidian runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | engine/ purity boundary | mitigate | engine-purity.test.js (Task 3) fails any require('obsidian') / window. / document. in engine/ — prevents the duplicate-engine-copy drift that motivated ENG-01 |
| T-05-02 | Information Disclosure | git mv of engine files | accept | Files move within the same repo; uncommitted working-tree changes ride along via git mv (verified semantics) — no data leaves the repo |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. esbuild install happens in Plan 05-05; esbuild@0.28.1 is already vendored in 3 existing plugin node_modules dirs (in-repo trust, version will be verified against existing package-lock.json there — no new external trust decision) |
</threat_model>

<verification>
- `node --test tests/export-base.test.js tests/export-med.test.js tests/export-plugin.test.js tests/constants.test.js tests/engine-purity.test.js` — all pass
- `git status --short` shows renames R for the 5 engine files and NO deletions of golden files
- `git diff --stat tests/golden` unchanged from before this plan (goldens byte-identical — nothing here touches them)
- Full suite still green except nothing: shared/schema/ remains in place, so flow tests (entity-templates, canvas-templates, schema) still pass unchanged
</verification>

<success_criteria>
- [ ] plugins/narrative-tool/src/engine/ contains export-engine.js, gd-format.js, med-format.js, gd-constants.js, med-constants.js
- [ ] gd-format.js / med-format.js import constants via relative './' paths; zero 'shared/' references remain in engine/
- [ ] shared/ contains only schema/ (constants relocated per D-03)
- [ ] 4 engine test files pass from the new import paths
- [ ] engine-purity.test.js guards the layer boundary
- [ ] No golden file modified, no engine behavior change
</success_criteria>

<output>
Create `.planning/phases/05-plugin-merge-bug-fixes/05-01-SUMMARY.md` when done
</output>
