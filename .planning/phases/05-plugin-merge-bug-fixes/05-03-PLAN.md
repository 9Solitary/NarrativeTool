---
phase: 05-plugin-merge-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - plugins/narrative-tool/src/commands/batch-export.js
  - plugins/narrative-tool/src/commands/auto-export.js
  - plugins/narrative-tool/src/commands/reference-validator.js
  - plugins/narrative-tool/src/commands/export-current.js
  - plugins/narrative-tool/src/commands/paths.js
  - plugins/narrative-tool/src/ui/settings.js
  - plugins/narrative-tool/src/ui/status-bar.js
  - plugins/narrative-tool/src/ui/modals.js
  - plugins/narrative-tool/src/ui/notify.js
  - plugins/narrative-tool/src/ui/nc-bridge.js
  - tests/batch-export.test.js
  - tests/auto-export.test.js
  - tests/reference-validator.test.js
  - tests/settings.test.js
  - tests/status-bar.test.js
  - tests/paths.test.js
autonomous: true
requirements: [ENG-01, BUG-02, BUG-03]

must_haves:
  truths:
    - "Batch export writes .dialogue files into the configured Export Path, not the vault root (BUG-02)"
    - "Auto export writes .dialogue files into the configured Export Path, not alongside the source (BUG-03)"
    - "Absolute export paths (e.g. D:/Godot/dialogues/) are written via fs; vault-relative via vault API — one shared path module used by both exporters and export-current"
    - "ui/ layer exists: deduped modals, notify wrapper, nc-bridge; settings/status-bar classes renamed for the merged plugin"
    - "Command/UI tests import from the merged layout and pass"
  artifacts:
    - path: "plugins/narrative-tool/src/commands/paths.js"
      provides: "Shared export-path decision + write module (kills the 3-copy duplication that caused B1/B2)"
      exports: ["isAbsoluteExportPath", "writeDialogueFile"]
    - path: "plugins/narrative-tool/src/commands/export-current.js"
      provides: "Deduped single-file export command (merges dialogue-export's absolute-path logic with narrative-project's status-bar feedback)"
      exports: ["exportCurrentDialogue"]
    - path: "plugins/narrative-tool/src/ui/modals.js"
      provides: "Deduped suggesters: FileSuggesterModal (canonical — replaces NcanvasFileSuggester), StringSuggesterModal, FolderSuggestModal, promptForInput"
    - path: "plugins/narrative-tool/src/ui/notify.js"
      provides: "notify(message, type) Notice wrapper (D-14)"
    - path: "plugins/narrative-tool/src/ui/nc-bridge.js"
      provides: "Optional-chained NC bridge (D-12); zero callers in v1.0 (D-13)"
  key_links:
    - from: "plugins/narrative-tool/src/commands/batch-export.js"
      to: "plugins/narrative-tool/src/commands/paths.js"
      via: "writeDialogueFile for output"
      pattern: "writeDialogueFile"
    - from: "plugins/narrative-tool/src/commands/auto-export.js"
      to: "plugins/narrative-tool/src/commands/paths.js"
      via: "writeDialogueFile for output"
      pattern: "writeDialogueFile"
    - from: "plugins/narrative-tool/src/ui/settings.js"
      to: "plugins/narrative-tool/src/ui/modals.js"
      via: "FolderSuggestModal import"
      pattern: "require\\(\\./modals\\)"
---

<objective>
Move the narrative-project command modules (batch-export, auto-export, reference-validator) into the merged commands/ layer, create the shared export-path module (fixing BUG-02 and BUG-03), dedupe the two duplicate "export current" commands into commands/export-current.js, and build the ui/ layer (deduped modals, notify wrapper, nc-bridge, renamed settings + status-bar).

Purpose: ENG-01 command/ui layering with the B1/B2 bug fixes riding along — these files are in motion. Per Pitfall 7, the fix must be a SHARED path module (absolute → fs, vault-relative → vault API, empty → alongside source) used by batch-export, auto-export, and export-current — three divergent copies existed today.

Output: commands/ and ui/ layers populated; both export paths honor the configured Export Path; renamed classes; tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md

Source files (current state — read before moving/modifying):
@plugins/narrative-project/src/batch-export.js
@plugins/narrative-project/src/auto-export.js
@plugins/narrative-project/src/reference-validator.js
@plugins/narrative-project/src/settings.js
@plugins/narrative-project/src/status-bar.js
@plugins/dialogue-export/src/main.js (exportCurrentDialogue method — the absolute-path implementation to dedupe, lines 57-156)
@plugins/flow-tools/src/main.js (FileSuggesterModal/StringSuggesterModal/promptForInput definitions, lines 39-127)

Test files whose require paths / assertions change:
@tests/batch-export.test.js
@tests/auto-export.test.js
@tests/reference-validator.test.js
@tests/settings.test.js
@tests/status-bar.test.js

<interfaces>
Current export path behavior to consolidate (from dialogue-export/src/main.js lines 100-125 — THE correct reference):
- exportDir = (settings.exportPath || '').trim()
- empty exportDir → output alongside source: activeFile.path.replace(/\.ncanvas$/, '.dialogue') via vault API
- absolute (path.isAbsolute || /^[A-Z]:[/\\]/i.test) → fs.mkdirSync(exportDir, {recursive:true}) + fs.writeFileSync(path.join(exportDir, outFilename), content, 'utf-8')
- else vault-relative → vault API create/modify at exportDir + '/' + outFilename

BUG-02 (batch-export.js current): writes outBasename at VAULT ROOT ignoring exportPath (lines 125-139)
BUG-03 (auto-export.js current): writes alongside source ignoring exportPath (line 77)

From plugins/narrative-project/src/settings.js:
- DEFAULT_SETTINGS (Object.freeze): exportPath: 'Exports', medEnabled: true, exportScope: '/'
- NarrativeProjectSettingTab class (rename to NarrativeToolSettingTab) + FolderSuggestModal (moves to ui/modals.js)

From plugins/narrative-project/src/status-bar.js:
- StatusBarManager; CSS class 'narrative-project-status' (rename to 'narrative-tool-status'); state strings stay English (Phase 8 sweeps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/paths.js, fix BUG-02/BUG-03, dedupe export-current</name>
  <files>
    plugins/narrative-tool/src/commands/paths.js
    plugins/narrative-tool/src/commands/batch-export.js
    plugins/narrative-tool/src/commands/auto-export.js
    plugins/narrative-tool/src/commands/reference-validator.js
    plugins/narrative-tool/src/commands/export-current.js
  </files>
  <read_first>
    plugins/narrative-project/src/batch-export.js (output section lines 125-139; keep the legacy-format upgrade and duplicate-basename prefix rule)
    plugins/narrative-project/src/auto-export.js (exportSingleFile output at line 77; keep setupAutoExport/teardownAutoExport signatures)
    plugins/dialogue-export/src/main.js (lines 57-156 — exportCurrentDialogue to dedupe)
    .planning/research/PITFALLS.md (Pitfall 7: absolute→fs, relative→vault, empty→alongside; Windows edge cases)
  </read_first>
  <action>
    Step 1 — git mv the three command modules:
    git mv plugins/narrative-project/src/batch-export.js plugins/narrative-tool/src/commands/batch-export.js
    git mv plugins/narrative-project/src/auto-export.js plugins/narrative-tool/src/commands/auto-export.js
    git mv plugins/narrative-project/src/reference-validator.js plugins/narrative-tool/src/commands/reference-validator.js

    Step 2 — create plugins/narrative-tool/src/commands/paths.js (new shared module; imports node:fs + node:path only — zero obsidian imports). Export exactly:
    - `isAbsoluteExportPath(p)`: returns true if path.isAbsolute(p) || /^[A-Z]:[/\\]/i.test(p). Document in a comment: drive-relative 'D:foo' is NOT absolute (keeps existing detection — intentional, documented decision).
    - `async writeDialogueFile(app, exportPath, outFilename, sourceFile, content)`: returns Promise<{mode: 'absolute'|'vault', path: string}>. Behavior (mirror dialogue-export main.js exactly):
      - const dir = (exportPath || '').trim();
      - if (!dir) → vault mode, path = sourceFile.path.replace(/\.ncanvas$/, '.dialogue') (alongside source); write via vault API create/modify.
      - if (isAbsoluteExportPath(dir)) → if (!fs.existsSync(dir)) fs.mkdirSync(dir, {recursive:true}); const abs = path.join(dir, outFilename); fs.writeFileSync(abs, content, 'utf-8'); return {mode:'absolute', path:abs}.
      - else vault-relative → ensure each segment exists (copy the ensureDirectory pattern from batch-export.js into paths.js as a local helper), then vault API create/modify at dir + '/' + outFilename; return {mode:'vault', path: dir + '/' + outFilename}.
      (paths.js is write-only — parsing/engine stay in the callers.)
    - module.exports = { isAbsoluteExportPath, writeDialogueFile }

    Step 3 — fix BUG-02 in plugins/narrative-tool/src/commands/batch-export.js:
    - Change the engine require from '../../dialogue-export/src/export-engine' to '../engine/export-engine'.
    - Import { writeDialogueFile } from './paths'.
    - Replace the output construction (currently lines 125-139: outBasename at vault root + vault-root create/modify) with: keep the outBasename + duplicate-basename prefix rule (outBasename = file.basename + '.dialogue'; if app.vault.getAbstractFileByPath(outBasename) → prefix parent dir name), then `const result = await writeDialogueFile(app, exportPath, outBasename, file, dialogueText);`. Keep the exported signature exportAllDialogues(app, exportPath, exportScope, medEnabled) and the legacy-format upgrade block unchanged.

    Step 4 — fix BUG-03 in plugins/narrative-tool/src/commands/auto-export.js:
    - Change the engine require to '../engine/export-engine'.
    - Import { writeDialogueFile } from './paths'.
    - In exportSingleFile, replace the alongside-source write (line 77 + the create/modify block) with: `const result = await writeDialogueFile(app, exportPath, file.basename + '.dialogue', file, dialogueText); return { success: true, path: result.path };`. Keep setupAutoExport/teardownAutoExport and the .ncanvas extension filter unchanged.

    Step 5 — create plugins/narrative-tool/src/commands/export-current.js (dedupe — merges dialogue-export's exportCurrentDialogue with narrative-project's status-bar feedback):
    - module.exports = { exportCurrentDialogue }; signature `async function exportCurrentDialogue(plugin)` (plugin exposes .app, .settings, .statusBar).
    - Logic: activeFile = plugin.app.workspace.getActiveFile() || plugin.app.workspace.activeLeaf?.view?.file; if not .ncanvas → FileSuggesterModal picker (from '../ui/modals') over vault .ncanvas files (empty list → notify('No .ncanvas files found in vault.') and return); parse JSON (parse failure → notify error and return); `const dialogueOutput = exportEngine(ncanvasJson, { medEnabled: plugin.settings.medEnabled })` (from '../engine/export-engine'); plugin.statusBar.setState('exporting', {count: 1}); `const result = await writeDialogueFile(plugin.app, plugin.settings.exportPath, activeFile.basename + '.dialogue', activeFile, dialogueOutput)` (from './paths'); on success plugin.statusBar.setState('success', {exported: 1, failed: 0}) + notify('Exported "' + title + '" → ' + result.path) + 5000ms setTimeout reverting statusBar to 'pending'; on error plugin.statusBar.setState('failure', {message: err.message}) + notify error. Use notify from '../ui/notify' for all user-facing messages (D-14).

    Step 6 — reference-validator.js: only the move; no content change.
  </action>
  <verify>
    <automated>node -e "const p=require('./plugins/narrative-tool/src/commands/paths'); const b=require('./plugins/narrative-tool/src/commands/batch-export'); const a=require('./plugins/narrative-tool/src/commands/auto-export'); const r=require('./plugins/narrative-tool/src/commands/reference-validator'); console.log(typeof p.writeDialogueFile, typeof p.isAbsoluteExportPath, typeof b.exportAllDialogues, typeof a.setupAutoExport, typeof r.validateReferences)"</automated>
  </verify>
  <acceptance_criteria>
    - plugins/narrative-tool/src/commands/paths.js exists; `grep -c "require('obsidian')\|require(\"obsidian\")" plugins/narrative-tool/src/commands/paths.js` returns 0 (pure node module)
    - `grep -c "writeDialogueFile" plugins/narrative-tool/src/commands/batch-export.js` ≥ 1 and same for auto-export.js; `grep -rn "dialogue-export" plugins/narrative-tool/src/commands/` returns zero matches
    - export-current.js exports exportCurrentDialogue — load-checked in Task 2's verify (it imports ui/modals + ui/notify, which are created in Task 2, so it cannot be smoke-required here)
    - Behavioral verification of BUG-02/BUG-03 lands in Task 3's verify (after test requires are re-pointed): paths.test.js asserts absolute/vault/empty branches; batch-export tests assert output lands under the exportPath dir (not vault root); auto-export tests assert output path ends with exportPath + '/' + basename
  </acceptance_criteria>
  <done>paths.js shared module in place; BUG-02/BUG-03 fixed in both exporters; export-current deduped</done>
</task>

<task type="auto">
  <name>Task 2: Build the ui/ layer — modals dedup, notify, nc-bridge, settings + status-bar renames</name>
  <files>
    plugins/narrative-tool/src/ui/modals.js
    plugins/narrative-tool/src/ui/notify.js
    plugins/narrative-tool/src/ui/nc-bridge.js
    plugins/narrative-tool/src/ui/settings.js
    plugins/narrative-tool/src/ui/status-bar.js
  </files>
  <read_first>
    plugins/flow-tools/src/main.js (FileSuggesterModal lines 71-96, StringSuggesterModal lines 39-64, promptForInput lines 103-127)
    plugins/dialogue-export/src/main.js (NcanvasFileSuggester lines 16-32 — dedup target)
    plugins/narrative-project/src/settings.js (FolderSuggestModal lines 17-33 + NarrativeProjectSettingTab lines 54-100)
    plugins/narrative-project/src/status-bar.js
    .planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <action>
    Step 1 — git mv settings.js and status-bar.js:
    git mv plugins/narrative-project/src/settings.js plugins/narrative-tool/src/ui/settings.js
    git mv plugins/narrative-project/src/status-bar.js plugins/narrative-tool/src/ui/status-bar.js

    Step 2 — create plugins/narrative-tool/src/ui/modals.js (dedup — canonical copies):
    - FileSuggesterModal: copy verbatim from flow-tools/src/main.js lines 71-96 (constructor(app, files, onChoose); empty query returns all files). This is the CANONICAL suggester — NcanvasFileSuggester from dialogue-export/src/main.js (lines 16-32) is functionally identical and MUST NOT be recreated; its call sites use FileSuggesterModal (Claude's discretion, documented).
    - StringSuggesterModal: copy verbatim from flow-tools/src/main.js lines 39-64.
    - promptForInput: copy verbatim from flow-tools/src/main.js lines 103-127.
    - FolderSuggestModal: copy from settings.js lines 17-33 (renderSuggestion keeps '(vault root)' text for '/').
    - module.exports = { FileSuggesterModal, StringSuggesterModal, FolderSuggestModal, promptForInput }

    Step 3 — create plugins/narrative-tool/src/ui/notify.js (D-14):
    - `const { Notice } = require('obsidian');`
    - `function notify(message, type)`: type defaults to 'info'; const notice = new Notice(message); if (type === 'error') notice.noticeEl.addClass('mod-error'); return notice. Export { notify }. Chinese strings are used for NEW messages added in this phase (e.g. the migration notice in Plan 05-04); existing English message strings are preserved verbatim — the full Chinese sweep is Phase 8 per D-14.

    Step 4 — create plugins/narrative-tool/src/ui/nc-bridge.js (D-12/D-13):
    - ~40-line optional-chained wrapper; the ONLY module allowed to reference window.NarrativeCanvasHost (ARCHITECTURE.md §3).
    - Functions: getNC() → (typeof window !== 'undefined') ? window.NarrativeCanvasHost : null; isNarrativeCanvasActive() → !!getNC(); one guarded accessor per NC capability, always optional-chained: chooseProjectFile, getProjectFile, ensureProjectFile, createProjectFile, showNotice — each shaped as `const nc = getNC(); if (!nc?.<fn>) return null; return nc.<fn>(...);`.
    - MUST NOT assign, replace, or delete window.NarrativeCanvasHost (comment the constraint).
    - Zero callers in v1.0 (D-13) — the file exists as future-proofing.

    Step 5 — rename in plugins/narrative-tool/src/ui/settings.js:
    - Class NarrativeProjectSettingTab → NarrativeToolSettingTab (class name, constructor, and export). DEFAULT_SETTINGS unchanged (keys exportPath/medEnabled/exportScope — migration compat per D-06).
    - Remove the local FolderSuggestModal class definition and import it instead: `const { FolderSuggestModal } = require('./modals');` (delete the old class block — it now lives in modals.js).

    Step 6 — rename in plugins/narrative-tool/src/ui/status-bar.js:
    - CSS class 'narrative-project-status' → 'narrative-tool-status' (constructor). All other content unchanged (state strings stay English until Phase 8).
  </action>
  <verify>
    <automated>node -e "const Module=require('node:module'),path=require('node:path'),orig=Module._resolveFilename; Module._resolveFilename=function(r,p,i,o){return (r==='obsidian'||r==='obsidian/')?path.join('tests','mocks','obsidian.js'):orig.call(this,r,p,i,o);}; const m=require('./plugins/narrative-tool/src/ui/modals'); const n=require('./plugins/narrative-tool/src/ui/notify'); const b=require('./plugins/narrative-tool/src/ui/nc-bridge'); const s=require('./plugins/narrative-tool/src/ui/settings'); const t=require('./plugins/narrative-tool/src/ui/status-bar'); const e=require('./plugins/narrative-tool/src/commands/export-current'); console.log(typeof m.FileSuggesterModal, typeof m.promptForInput, typeof n.notify, typeof b.isNarrativeCanvasActive, typeof s.NarrativeToolSettingTab, typeof t.StatusBarManager, typeof e.exportCurrentDialogue)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NarrativeToolSettingTab" plugins/narrative-tool/src/ui/settings.js` ≥ 2 (class + export); `grep -c "NarrativeProjectSettingTab" plugins/narrative-tool/src/ui/settings.js` returns 0
    - `grep -c "narrative-tool-status" plugins/narrative-tool/src/ui/status-bar.js` ≥ 1; `grep -c "narrative-project-status" plugins/narrative-tool/src/ui/status-bar.js` returns 0
    - plugins/narrative-tool/src/ui/modals.js exports FileSuggesterModal, StringSuggesterModal, FolderSuggestModal, promptForInput; NcanvasFileSuggester is NOT recreated anywhere in plugins/narrative-tool/src
    - plugins/narrative-tool/src/ui/notify.js exports notify; nc-bridge.js exports getNC/isNarrativeCanvasActive and never assigns window.NarrativeCanvasHost (`grep -c "NarrativeCanvasHost =" plugins/narrative-tool/src/ui/nc-bridge.js` returns 0)
  </acceptance_criteria>
  <done>ui/ layer complete: deduped modals, notify wrapper, nc-bridge, renamed NarrativeToolSettingTab + narrative-tool-status class</done>
</task>
<task type="auto">
  <name>Task 3: Re-point command/UI test imports and add paths.test.js</name>
  <files>
    tests/batch-export.test.js
    tests/auto-export.test.js
    tests/reference-validator.test.js
    tests/settings.test.js
    tests/status-bar.test.js
    tests/paths.test.js
  </files>
  <read_first>
    tests/batch-export.test.js (require path line 155; MockVault class at top of file — reuse its shape for paths.test.js)
    tests/auto-export.test.js (require paths at lines 172, 246, 354; assertion of output location)
    tests/reference-validator.test.js (require at line 147)
    tests/settings.test.js (requires at lines 44-112; NarrativeProjectSettingTab assertions at lines 84-99)
    tests/status-bar.test.js (require at line 46; class assertion at lines 58-59)
  </read_first>
  <action>
    Update require paths and rename assertions. Exact replacements:

    1. tests/batch-export.test.js: `'../plugins/narrative-project/src/batch-export'` → `'../plugins/narrative-tool/src/commands/batch-export'` (line 155). Then review every assertion about output location: any test asserting the .dialogue lands at vault root must now assert it lands under the exportPath directory passed to exportAllDialogues (e.g. exportPath 'Exports' → file at 'Exports/<basename>.dialogue', folder created). Fix the assertions, do not loosen them.
    2. tests/auto-export.test.js: `'../plugins/narrative-project/src/auto-export'` → `'../plugins/narrative-tool/src/commands/auto-export'` (all 3 occurrences). Review output-location assertions: exportSingleFile now writes into exportPath (default 'Exports') instead of alongside the source — update any assertion expecting the .dialogue next to the .ncanvas to expect '<exportPath>/<basename>.dialogue'.
    3. tests/reference-validator.test.js: `'../plugins/narrative-project/src/reference-validator'` → `'../plugins/narrative-tool/src/commands/reference-validator'` (line 147). No assertion changes.
    4. tests/settings.test.js: `'../plugins/narrative-project/src/settings'` → `'../plugins/narrative-tool/src/ui/settings'` (all occurrences); update the class assertions: `NarrativeProjectSettingTab` → `NarrativeToolSettingTab` (both the require destructure and the name assertions at lines 84-99).
    5. tests/status-bar.test.js: `'../plugins/narrative-project/src/status-bar'` → `'../plugins/narrative-tool/src/ui/status-bar'` (line 46); update the CSS class assertion at lines 58-59 from 'narrative-project-status' to 'narrative-tool-status'. Do NOT touch the state string assertions ('Narrative Toolchain', 'Exporting', 'exported', 'Parse error') — strings stay English until Phase 8.

    Create tests/paths.test.js (new — node:test + node:assert; follows the Module._resolveFilename obsidian-mock pattern only if needed — paths.js imports no obsidian, so no mock needed):
    - MockVault: copy the in-memory vault class shape from batch-export.test.js (files map, getFiles/getAbstractFileByPath/create/modify/createFolder/read).
    - Test group A — isAbsoluteExportPath: 'D:\\Godot\\dialogues\\' (backslashes) → true; 'D:/Godot/dialogues' (forward) → true; 'Exports' → false; 'Exports/Sub' → false; 'D:foo' (drive-relative) → false (documented decision); '' → false.
    - Test group B — writeDialogueFile vault-relative: exportPath 'Exports' + MockVault → returns {mode:'vault', path:'Exports/<name>.dialogue'}; file exists in mock vault under Exports/; nested 'Exports/Sub' creates folders.
    - Test group C — writeDialogueFile absolute: use fs.mkdtempSync(path.join(os.tmpdir(), 'nt-paths-')) as exportPath → returns {mode:'absolute', path: <tmp>/<name>.dialogue}; assert the file EXISTS ON DISK (fs.existsSync + content readback). Clean up with fs.rmSync(tmp, {recursive:true, force:true}) in a final cleanup (node:test after() or try/finally).
    - Test group D — empty exportPath → alongside source: sourceFile {path:'Dialogues/Innkeeper.ncanvas', basename:'Innkeeper'} → vault mode, path 'Dialogues/Innkeeper.dialogue'.
    - Test group E — Windows path variants: writeDialogueFile with exportPath 'D:\\Godot\\dialogues\\' (trailing backslash) behaves like absolute (on any OS: isAbsoluteExportPath('D:\\Godot\\dialogues\\') true; the write itself uses fs — on non-Windows the literal 'D:' folder is created in cwd — so for the fs-write tests use mkdtemp; for pure detection assertions use the drive-letter forms).
  </action>
  <verify>
    <automated>node --test tests/paths.test.js tests/batch-export.test.js tests/auto-export.test.js tests/reference-validator.test.js tests/settings.test.js tests/status-bar.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "narrative-project/src" tests/batch-export.test.js tests/auto-export.test.js tests/reference-validator.test.js tests/settings.test.js tests/status-bar.test.js` returns zero matches
    - `grep -c "narrative-tool-status" tests/status-bar.test.js` ≥ 1; `grep -c "NarrativeToolSettingTab" tests/settings.test.js` ≥ 1
    - tests/paths.test.js covers the absolute fs branch (temp dir file-on-disk assertion), vault-relative branch, empty→alongside branch, and Windows path detection; all 6 test files pass (fail 0)
  </acceptance_criteria>
  <done>All command/UI tests import from merged paths and pass; paths.test.js proves BUG-02/BUG-03 behavior</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commands/* → vault API / fs | Export writes cross from vault-relative paths (vault API) to absolute filesystem paths (node fs) — user-configured destination, desktop-only plugin |
| ui/notify → Notice | User-facing messages; strings only |
| nc-bridge → window.NarrativeCanvasHost | Read-only optional-chained access; must never assign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | commands/paths.js absolute writes | mitigate | Absolute branch only writes to user-configured exportPath via fs (mkdirSync recursive + writeFileSync); keeps batch-export's duplicate-basename prefix rule; isAbsoluteExportPath is the single decision point — tested for Windows drive forms in paths.test.js group A/E |
| T-05-06 | Spoofing | legacy data.json reads (future migration consumer) | accept | Migration in Plan 05-04 reads user's own plugin data.json; try/catch per file; merge over Object.assign with DEFAULT_SETTINGS (known keys only win) |
| T-05-07 | Tampering | ui/modals.js dedup | mitigate | All four modal classes copied verbatim from existing tested code; FileSuggesterModal canonical (NcanvasFileSuggester intentionally NOT duplicated — its callers in export-current use FileSuggesterModal) |
| T-05-08 | Spoofing | nc-bridge.js | accept | Zero callers in v1.0 (D-13); module only reads window.NarrativeCanvasHost via optional chaining, never assigns — constraint comment in file |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (esbuild install deferred to Plan 05-05; esbuild@0.28.1 already vendored in-repo — no new trust decision) |
</threat_model>

<verification>
- `node --test tests/paths.test.js tests/batch-export.test.js tests/auto-export.test.js tests/reference-validator.test.js tests/settings.test.js tests/status-bar.test.js` — all pass
- `git status --short` shows renames R for the 5 moved modules; no golden .dialogue file touched
- `git diff --stat tests/golden` unchanged (byte-identical contract)
- Engine tests from Plan 05-01 and flow tests from Plan 05-02 still pass (spot-check `node --test tests/constants.test.js tests/schema.test.js`)
</verification>

<success_criteria>
- [ ] commands/ layer: batch-export, auto-export, reference-validator, export-current, paths
- [ ] ui/ layer: modals (deduped), notify, nc-bridge, settings (renamed NarrativeToolSettingTab), status-bar (renamed narrative-tool-status class)
- [ ] BUG-02 closed: batch export writes into Export Path (tested)
- [ ] BUG-03 closed: auto export writes into Export Path (tested)
- [ ] paths.test.js proves absolute (fs, on-disk), vault-relative, empty→alongside, and Windows path behaviors
- [ ] No golden .dialogue modified; all touched test files green
</success_criteria>

<output>
Create `.planning/phases/05-plugin-merge-bug-fixes/05-03-SUMMARY.md` when done
</output>
