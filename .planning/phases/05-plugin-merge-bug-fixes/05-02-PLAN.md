---
phase: 05-plugin-merge-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - plugins/narrative-tool/src/flow/entity-templates.js
  - plugins/narrative-tool/src/flow/canvas-templates.js
  - plugins/narrative-tool/src/flow/canvas-utils.js
  - plugins/narrative-tool/src/flow/navigation.js
  - plugins/narrative-tool/src/engine/schema/character.js
  - plugins/narrative-tool/src/engine/schema/location.js
  - plugins/narrative-tool/src/engine/schema/quest.js
  - plugins/narrative-tool/src/engine/schema/item.js
  - tests/entity-templates.test.js
  - tests/canvas-templates.test.js
  - tests/schema.test.js
  - tests/fixtures/expected-quest.md
autonomous: true
requirements: [ENG-01, BUG-01]

must_haves:
  truths:
    - "Flow modules (entity-templates, canvas-templates, canvas-utils, navigation) live in plugins/narrative-tool/src/flow/ and import schema from engine/schema/ (D-03)"
    - "User can create a Quest entity: createQuestMd exists, is exported, and produces the golden expected-quest.md content (BUG-01)"
    - "Flow/schema tests import from the new locations and pass"
  artifacts:
    - path: "plugins/narrative-tool/src/flow/entity-templates.js"
      provides: "Entity templates incl. restored createQuestMd (BUG-01)"
      exports: ["createCharacterMd", "createLocationMd", "createQuestMd", "createItemMd"]
    - path: "plugins/narrative-tool/src/engine/schema/quest.js"
      provides: "Quest schema relocated per D-03"
      exports: ["QuestFields", "QuestRequired"]
    - path: "tests/fixtures/expected-quest.md"
      provides: "Quest golden fixture restored from git HEAD (deleted in working tree — the BUG-01 regression)"
  key_links:
    - from: "plugins/narrative-tool/src/flow/entity-templates.js"
      to: "plugins/narrative-tool/src/engine/schema/character.js"
      via: "require('../engine/schema/character')"
      pattern: "require\\(\\.\\./engine/schema/character\\)"
    - from: "tests/entity-templates.test.js"
      to: "plugins/narrative-tool/src/flow/entity-templates"
      via: "require path update"
      pattern: "require\\(\\.\\./plugins/narrative-tool/src/flow/entity-templates\\)"
---

<objective>
Move the flow-tools modules (entity-templates, canvas-templates, canvas-utils, navigation) into the merged plugin's flow/ layer, relocate shared/schema/ into engine/schema/ per D-03, and fix BUG-01: restore the regressed `createQuestMd` template function with its golden fixture and tests.

Purpose: ENG-01 file movement for the flow layer (mechanical, zero behavior change), plus the BUG-01 ride-along fix (the Quest template was removed from entity-templates.js in the working tree; the full implementation, golden fixture, and tests exist in git history and must be restored).

Output: flow/ layer populated; schema relocated; createQuestMd restored and tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md

Source files (current state — read before moving):
@plugins/flow-tools/src/entity-templates.js
@plugins/flow-tools/src/canvas-templates.js
@plugins/flow-tools/src/canvas-utils.js
@plugins/flow-tools/src/navigation.js
@shared/schema/character.js
@shared/schema/location.js
@shared/schema/quest.js
@shared/schema/item.js

Restoration references (git history — read before restoring):
- `git show 4deaef8:plugins/flow-tools/src/entity-templates.js` — the createQuestMd implementation (lines ~155-215) and its export in module.exports
- `git show HEAD:tests/fixtures/expected-quest.md` — the deleted Quest golden fixture
- `git show HEAD:tests/entity-templates.test.js` — the removed Quest test blocks (golden match test, tags test, exports test)

Test files whose require paths change:
@tests/entity-templates.test.js
@tests/canvas-templates.test.js
@tests/schema.test.js

<interfaces>
From plugins/flow-tools/src/canvas-templates.js (module.exports):
- FLOW_TEMPLATES = { CHAPTER: 'chapter', QUEST: 'quest', WORLD_EVENT: 'world-event' }
- FRAGMENT_TEMPLATES = { QUEST_DETAIL: 'quest-detail', SCENE_BREAKDOWN: 'scene-breakdown' }
- createFlowCanvas(templateType, params, options) -> JSON string (options.generateNodeId / options.generateEdgeId for tests)
- createFlowFragment(templateType, params, options) -> JSON string

From plugins/flow-tools/src/canvas-utils.js (module.exports):
- generateNodeId() -> 16-char hex, createCanvas(), addNodeToCanvas(canvas, node), addDialogueNodeToCanvas(canvas, path, position, idGenerator)

From plugins/flow-tools/src/navigation.js (module.exports):
- openDialogueFile(app, path), openFlowCanvas(app, path), openFileInSplit(app, path) — all async, Notice on error

From plugins/flow-tools/src/entity-templates.js — CURRENT exports (regressed):
- createCharacterMd, createLocationMd, createItemMd — schema imports at lines 10-12: require('../../../shared/schema/character'|'location'|'item')

Restored createQuestMd contract (from 4deaef8, do not re-derive):
- params: { id, name, description, questType, prerequisites, stages, giverCharacterId, involvedLocationIds, notes }
- frontmatter keys: id, name, description, quest_type, prerequisites, stages, giver_character_id, involved_location_ids + tags: [quest]
- body sections: `# <name>`, description, `**Type:** <questType>`, `## 任务阶段` (numbered stages), `## 关联角色` ([[giverCharacterId]]), `## 关联地点` ([[locId]] per entry)
- imports QuestFields, QuestRequired from shared/schema/quest (→ '../engine/schema/quest' after move)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move flow modules and schema into the merged layout (git mv + import fix)</name>
  <files>
    plugins/narrative-tool/src/flow/entity-templates.js
    plugins/narrative-tool/src/flow/canvas-templates.js
    plugins/narrative-tool/src/flow/canvas-utils.js
    plugins/narrative-tool/src/flow/navigation.js
    plugins/narrative-tool/src/engine/schema/character.js
    plugins/narrative-tool/src/engine/schema/location.js
    plugins/narrative-tool/src/engine/schema/quest.js
    plugins/narrative-tool/src/engine/schema/item.js
  </files>
  <read_first>
    plugins/flow-tools/src/entity-templates.js (schema imports at lines 10-12)
    plugins/flow-tools/src/canvas-templates.js (internal require './canvas-utils')
    plugins/flow-tools/src/navigation.js (require('obsidian') for Notice — stays, it is an Obsidian-coupled flow/ module)
    .planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Move files with `git mv` (preserves history and any uncommitted changes — do NOT cp+delete):

    git mv plugins/flow-tools/src/entity-templates.js plugins/narrative-tool/src/flow/entity-templates.js
    git mv plugins/flow-tools/src/canvas-templates.js plugins/narrative-tool/src/flow/canvas-templates.js
    git mv plugins/flow-tools/src/canvas-utils.js plugins/narrative-tool/src/flow/canvas-utils.js
    git mv plugins/flow-tools/src/navigation.js plugins/narrative-tool/src/flow/navigation.js
    git mv shared/schema/character.js plugins/narrative-tool/src/engine/schema/character.js
    git mv shared/schema/location.js plugins/narrative-tool/src/engine/schema/location.js
    git mv shared/schema/quest.js plugins/narrative-tool/src/engine/schema/quest.js
    git mv shared/schema/item.js plugins/narrative-tool/src/engine/schema/item.js

    (engine/schema/ does not exist yet — create the directory first; the moved files are its only content. shared/ is now fully relocated — it should no longer exist after this task.)

    After the moves, edit ONLY import paths (no behavior change):

    1. plugins/narrative-tool/src/flow/entity-templates.js: change the three schema requires at lines 10-12 from require('../../../shared/schema/character') to require('../engine/schema/character'), 'location' and 'item' likewise. (The Quest import is added in Task 2.)
    2. plugins/narrative-tool/src/flow/canvas-templates.js: require('./canvas-utils') — sibling path survives the move unchanged, do NOT touch.
    3. plugins/narrative-tool/src/flow/navigation.js: require('obsidian') — stays (flow/ is Obsidian-coupled per ARCHITECTURE.md layer rules).

    Do NOT touch plugins/narrative-tool/src/flow/canvas-utils.js or navigation.js content. Do NOT touch tests yet (Task 3).
  </action>
  <verify>
    <automated>node -e "const t=require('./plugins/narrative-tool/src/flow/entity-templates'); const c=require('./plugins/narrative-tool/src/flow/canvas-templates'); const u=require('./plugins/narrative-tool/src/flow/canvas-utils'); const n=require('./plugins/narrative-tool/src/flow/navigation'); const q=require('./plugins/narrative-tool/src/engine/schema/quest'); console.log(typeof t.createCharacterMd, typeof c.createFlowCanvas, typeof u.generateNodeId, typeof n.openFlowCanvas, typeof q.QuestFields)"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short plugins/narrative-tool/src/flow plugins/narrative-tool/src/engine/schema shared` shows 8 renames (R); shared/ no longer exists on disk (`test ! -d shared` on POSIX or `dir shared` fails on Windows)
    - `grep -c "shared/" plugins/narrative-tool/src/flow/entity-templates.js` returns 0; `grep -c "require('../engine/schema/" plugins/narrative-tool/src/flow/entity-templates.js` returns 3
    - The smoke require command above prints function types for all five modules
  </acceptance_criteria>
  <done>Flow modules and schema relocated; entity-templates.js imports engine/schema; shared/ deleted from the repo</done>
</task>

<task type="auto">
  <name>Task 2: BUG-01 — restore createQuestMd, its golden fixture, and its tests</name>
  <files>
    plugins/narrative-tool/src/flow/entity-templates.js
    tests/fixtures/expected-quest.md
    tests/entity-templates.test.js
  </files>
  <read_first>
    plugins/narrative-tool/src/flow/entity-templates.js (current state after Task 1)
    Run `git show 4deaef8:plugins/flow-tools/src/entity-templates.js` and extract the createQuestMd section (function + its placement between createLocationMd and createItemMd + module.exports entry)
    Run `git show HEAD:tests/fixtures/expected-quest.md` for the golden fixture content
    Run `git show HEAD:tests/entity-templates.test.js` for the removed Quest test blocks
  </read_first>
  <action>
    Restore the Quest template regression (BUG-01) using the git-history implementations verbatim (do not rewrite the template — tests pin its exact output):

    1. plugins/narrative-tool/src/flow/entity-templates.js:
       a. Add the import alongside the existing schema imports (line 10-12 area): `const { QuestFields, QuestRequired } = require('../engine/schema/quest');`
       b. Insert the complete createQuestMd function (verbatim from `git show 4deaef8:plugins/flow-tools/src/entity-templates.js` lines ~155-215) between createLocationMd and createItemMd. The function signature and body must match the reference exactly: params { id, name, description, questType, prerequisites, stages, giverCharacterId, involvedLocationIds, notes }; frontmatter keys id, name, description, quest_type, prerequisites, stages, giver_character_id, involved_location_ids; buildFrontmatter(lines, 'quest'); body `# <name>`, description, `**Type:** <questType>`, `## 任务阶段` numbered list, `## 关联角色` [[giverCharacterId]], `## 关联地点` [[locId]] entries.
       c. Add createQuestMd to module.exports (between createLocationMd and createItemMd).
    2. Restore the deleted fixture: `git checkout HEAD -- tests/fixtures/expected-quest.md` (the working-tree deletion of this file IS the BUG-01 regression; restoring from HEAD is correct — verify its content matches what createQuestMd produces).
    3. Restore the removed Quest tests in tests/entity-templates.test.js: copy the Quest test blocks verbatim from `git show HEAD:tests/entity-templates.test.js` — (a) the golden-match test 'should match golden file expected-quest.md' (uses readGolden('expected-quest.md') — the readGolden helper exists in the current file), (b) the 'quest template should contain tags: [quest]' test, (c) the quest entries in the exports test (checks all four template functions are exported) and the quoted-name test. Update ONLY the require paths inside the restored blocks from '../plugins/flow-tools/src/entity-templates' to '../plugins/narrative-tool/src/flow/entity-templates'. Do not alter any existing (non-quest) tests.
  </action>
  <verify>
    <automated>node --test tests/entity-templates.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function createQuestMd" plugins/narrative-tool/src/flow/entity-templates.js` returns 1; `grep -A6 "module.exports" plugins/narrative-tool/src/flow/entity-templates.js | grep -c "createQuestMd"` returns 1
    - `grep -c "require('../engine/schema/quest')" plugins/narrative-tool/src/flow/entity-templates.js` returns 1
    - tests/fixtures/expected-quest.md exists on disk and its content matches `git show HEAD:tests/fixtures/expected-quest.md` (byte-identical)
    - node --test tests/entity-templates.test.js: pass count increased vs. the pre-restore run; fail 0; the golden-match quest test passes (proves createQuestMd output == expected-quest.md)
  </acceptance_criteria>
  <done>createQuestMd restored and exported; expected-quest.md fixture restored; quest tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Re-point flow and schema test imports</name>
  <files>
    tests/entity-templates.test.js
    tests/canvas-templates.test.js
    tests/schema.test.js
  </files>
  <read_first>
    tests/entity-templates.test.js (all require paths — lines 33-261 area)
    tests/canvas-templates.test.js (lines 51, 58)
    tests/schema.test.js (import path constants at lines 92, 128, 161, 197 and assertHasJSDoc path args at lines 119, 152, 188, 222)
  </read_first>
  <action>
    Update require paths in the three test files. Exact replacements:

    1. tests/entity-templates.test.js: every `'../plugins/flow-tools/src/entity-templates'` → `'../plugins/narrative-tool/src/flow/entity-templates'` (all occurrences; the restored quest blocks use the same new path per Task 2)
    2. tests/canvas-templates.test.js: `'../plugins/flow-tools/src/canvas-utils'` → `'../plugins/narrative-tool/src/flow/canvas-utils'` (line 51) and `'../plugins/flow-tools/src/canvas-templates'` → `'../plugins/narrative-tool/src/flow/canvas-templates'` (line 58)
    3. tests/schema.test.js:
       a. The four `path` constants: `'../shared/schema/character.js'` → `'../plugins/narrative-tool/src/engine/schema/character.js'` (line 92), 'location.js' (line 128), 'quest.js' (line 161), 'item.js' (line 197) likewise
       b. The four assertHasJSDoc path args: `'shared/schema/character.js'` → `'plugins/narrative-tool/src/engine/schema/character.js'` (line 119), 'location.js' (152), 'quest.js' (188), 'item.js' (222) likewise — note these are repo-relative (join(__dirname, '..', relativePath)) so NO leading ../ prefix

    Do NOT edit assertions — only paths. Do NOT touch tests/constants.test.js (already updated in Plan 05-01).
  </action>
  <verify>
    <automated>node --test tests/entity-templates.test.js tests/canvas-templates.test.js tests/schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "plugins/flow-tools\|'../shared/schema/\|shared/schema/" tests/entity-templates.test.js tests/canvas-templates.test.js tests/schema.test.js` returns zero matches
    - All three test files pass (fail 0) — entity-templates (incl. restored quest tests), canvas-templates, schema
  </acceptance_criteria>
  <done>All flow/schema tests import from the merged layout and pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow/ modules → engine/schema | Schema data flow after D-03 relocation; frozen template objects |
| tests → flow/ modules | node:test imports flow modules (navigation.js requires obsidian → mock interception exists in canvas tests only; navigation is NOT imported by tests in this plan — do not add navigation imports here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | entity-templates.js | mitigate | createQuestMd restored verbatim from git history (4deaef8) — content pinned by expected-quest.md golden; no rewrite drift. Path-traversal sanitization lives in main.js (Plan 05-04), not in the template module |
| T-05-04 | Spoofing | schema relocation | mitigate | schema.test.js asserts frozen templates + exact key sets against the relocated files; any move corruption fails the suite |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (see Plan 05-01 register) |
</threat_model>

<verification>
- `node --test tests/entity-templates.test.js tests/canvas-templates.test.js tests/schema.test.js` — all pass
- `git status --short` shows 8 renames and the restored fixture; no golden .dialogue file touched
- `git diff --stat tests/golden` unchanged (byte-identical contract holds)
- Engine tests from Plan 05-01 still pass (`node --test tests/export-base.test.js tests/constants.test.js` spot-check)
</verification>

<success_criteria>
- [ ] flow/ layer populated with 4 modules; shared/ gone (schema relocated to engine/schema/)
- [ ] createQuestMd restored, exported, golden-matched (BUG-01 closed)
- [ ] entity-templates, canvas-templates, schema test files green from new import paths
- [ ] No golden .dialogue modified
</success_criteria>

<output>
Create `.planning/phases/05-plugin-merge-bug-fixes/05-02-SUMMARY.md` when done
</output>
