---
phase: 05-plugin-merge-bug-fixes
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - plugins/dialogue-export/ (deleted)
  - plugins/narrative-project/ (deleted)
  - plugins/flow-tools/ (deleted)
autonomous: true
requirements: [ENG-01]

must_haves:
  truths:
    - "No legacy plugin code remains in the repo — tests cannot silently import stale copies (Pitfall 6 item 5)"
    - "The merged plugin builds to a single main.js bundle containing all 10 narrative-tool: command IDs"
    - "Full test suite passes from the clean single-plugin state; 9 golden files byte-identical (unchanged vs. phase start)"
  artifacts:
    - path: "plugins/narrative-tool/main.js"
      provides: "esbuild build output (gitignored per plugins/*/main.js rule)"
      contains: "narrative-tool:"
    - path: "plugins/narrative-tool/package-lock.json"
      provides: "Pinned esbuild devDependency (npm install)"
  key_links:
    - from: "plugins/narrative-tool/src/main.js"
      to: "plugins/narrative-tool/main.js"
      via: "esbuild.config.mjs single-entry build"
      pattern: "narrative-tool:"
---

<objective>
Delete the three legacy plugin directories (dialogue-export, narrative-project, flow-tools), install the merged plugin's single dependency (esbuild), build the merged plugin to main.js, and run the full release verification: complete test suite green, golden files byte-identical, command inventory present in the bundle.

Purpose: ENG-01 final gate. The regression contract (203+ tests green from a clean state, 9 goldens byte-identical, command palette complete) can only be truthfully verified AFTER the old dirs are gone — while they linger, a missed test import could silently resolve to stale copies and pass (Pitfall 6.5). The build proves the merged plugin is loadable as a single artifact.

Output: Repo contains exactly one plugin (narrative-tool); its bundle builds; the full suite is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-plugin-merge-bug-fixes/05-CONTEXT.md

State entering this plan (from Plans 05-01..04):
- plugins/narrative-tool/src/ contains engine/ (incl. constants + schema per D-03), commands/ (paths, export-current, batch-export, auto-export, reference-validator), flow/ (entity-templates with createQuestMd, canvas-templates, canvas-utils, navigation with findFlowCanvasForDialogue), ui/ (modals, notify, nc-bridge, settings, status-bar), main.js (NarrativeToolPlugin), styles.css
- plugins/narrative-tool/{manifest.json, esbuild.config.mjs, package.json} exist; package.json declares esbuild ^0.28.1
- Legacy dirs still contain: src/ remnants (dialogue-export/src/main.js, narrative-project/src/main.js, flow-tools/src/main.js + styles.css), per-plugin configs (manifest.json, esbuild.config.mjs, package.json, package-lock.json), plus gitignored node_modules/ and built main.js bundles

Git facts:
- node_modules/ and built plugins/*/main.js are gitignored (never committed) — see .gitignore
- `git ls-files plugins` currently lists 33 tracked files across the 3 legacy plugins

Verification baseline (capture BEFORE task 2's suite run):
- git status --porcelain tests/golden  (expected: the pre-existing working-tree modifications from phase start — the merge itself must add NO new entries)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three legacy plugin directories</name>
  <files>
    plugins/dialogue-export/ (deleted)
    plugins/narrative-project/ (deleted)
    plugins/flow-tools/ (deleted)
  </files>
  <read_first>
    .gitignore (node_modules/ and plugins/*/main.js are untracked — handled by rm -rf, not git rm)
    Run `git ls-files plugins/dialogue-export plugins/narrative-project plugins/flow-tools` to see exactly what is tracked
  </read_first>
  <action>
    Remove all three legacy plugin directories, tracked and untracked content:

    1. `git rm -r plugins/dialogue-export plugins/narrative-project plugins/flow-tools` — removes the 33 tracked files (src/, manifest.json, esbuild.config.mjs, package.json, package-lock.json, flow-tools/src/styles.css, narrative-project/styles.css).
    2. `rm -rf plugins/dialogue-export plugins/narrative-project plugins/flow-tools` — removes the gitignored leftovers (node_modules/, built main.js bundles) so the directories no longer exist on disk.

    Verify with `ls plugins/` — must show ONLY narrative-tool. Also verify no test or source file references the old paths via require: `grep -rn "require(.*dialogue-export\|require(.*narrative-project\|require(.*flow-tools" tests/*.test.js tests/mocks plugins/narrative-tool/src --include="*.js"` — expected: zero require-path matches (all import paths updated in Plans 01-03). NOTE: the sweep is scoped to require-path patterns only — bare string literals such as '.obsidian/plugins/narrative-project/data.json' intentionally remain in tests/merge-smoke.test.js (the migration mock adapter) and inside main.js's migration code (vault.adapter.read paths); those are runtime strings, not require paths, and must not count as matches.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readdirSync('./plugins'); console.log(d); if (d.length!==1 || d[0]!=='narrative-tool') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `ls plugins/` (or readdirSync) shows exactly ['narrative-tool']
    - `git ls-files plugins/dialogue-export plugins/narrative-project plugins/flow-tools` returns nothing (all tracked files removed)
    - `grep -rn "require(.*dialogue-export\|require(.*narrative-project\|require(.*flow-tools" tests/ plugins/narrative-tool/src` returns zero require-path matches; the only 'narrative-project'/'dialogue-export'/'flow-tools' strings in plugins/narrative-tool/src are the migration data.json path literals inside main.js
  </acceptance_criteria>
  <done>All three legacy plugin directories deleted (tracked + untracked content); plugins/ contains only narrative-tool</done>
</task>
<task type="auto">
  <name>Task 2: Install esbuild, build the merged plugin, run full release verification</name>
  <files>
    plugins/narrative-tool/package-lock.json
    plugins/narrative-tool/main.js (build artifact)
  </files>
  <read_first>
    plugins/dialogue-export/package-lock.json was deleted in Task 1 — before deleting, note the esbuild version pinned there (expected 0.28.x) and verify the merged install matches
    plugins/narrative-tool/esbuild.config.mjs (Plan 05-04 output)
    plugins/narrative-tool/package.json (Plan 05-04 output)
  </read_first>
  <action>
    Step 1 — install the single devDependency:
    cd plugins/narrative-tool && npm install
    Verify the resolved esbuild version aligns with what the legacy lockfiles pinned (0.28.x): `npm ls esbuild`. esbuild is already trusted in-repo (vendored in 3 legacy plugin node_modules) — this is not a new package; the version-alignment check is the legitimacy verification (T-05-SC).

    Step 2 — build the merged plugin:
    cd plugins/narrative-tool && node esbuild.config.mjs
    Expected: build completes with no errors and produces plugins/narrative-tool/main.js (gitignored).

    Step 3 — command inventory in the bundle:
    `grep -c "narrative-tool:" plugins/narrative-tool/main.js` must be ≥ 10 (the esbuild bundle contains all command ID strings).
    Also assert the bundle does NOT contain legacy plugin ids as command prefixes: `grep -c "dialogue-export:\|flow-tools:\|narrative-project:" plugins/narrative-tool/main.js` returns 0.

    Step 4 — full suite from the clean single-plugin state (run from repo root):
    `node --test tests/*.test.js`
    Expected: all test files pass (the 234 pre-merge tests + new engine-purity, paths, merge-smoke, navigation tests). Record the final pass count in the SUMMARY.

    Step 5 — golden byte-identical contract:
    Re-run `git status --porcelain tests/golden` and compare with the baseline captured at plan start — the output must be IDENTICAL (the merge phase adds no new golden modifications; the pre-existing working-tree modifications from phase start remain the only entries).

    Step 6 — engine purity guard still green: `node --test tests/engine-purity.test.js` passes against the final engine/ layout.
  </action>
  <verify>
    <automated>node --test tests/engine-purity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - npm install succeeds; `npm ls esbuild` in plugins/narrative-tool resolves to 0.28.x (matches legacy lockfile pin)
    - esbuild build completes with exit code 0; plugins/narrative-tool/main.js exists and is non-empty
    - `grep -c "narrative-tool:" plugins/narrative-tool/main.js` ≥ 10; `grep -c "dialogue-export:\|flow-tools:\|narrative-project:" plugins/narrative-tool/main.js` returns 0
    - `node --test tests/*.test.js` from repo root: fail 0 (record pass count)
    - `git status --porcelain tests/golden` output identical to the baseline captured at plan start
  </acceptance_criteria>
  <done>Legacy plugins deleted; merged plugin builds; full suite green; goldens byte-identical; bundle carries all 10 narrative-tool: command IDs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → npm registry | Single devDependency install (esbuild) into plugins/narrative-tool |
| old plugin dirs → disk | Deletion of tracked + gitignored files (node_modules, built bundles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | legacy dir deletion | mitigate | git rm -r removes tracked files; rm -rf removes gitignored leftovers; post-check `ls plugins/` shows only narrative-tool; grep sweep proves no stale require paths remain (Pitfall 6.5) |
| T-05-14 | Spoofing | stale test imports | mitigate | Full suite run AFTER deletion — any test still importing old paths fails loudly instead of resolving to a stale copy |
| T-05-15 | Tampering | esbuild install | mitigate | esbuild@0.28.1 already vendored in 3 legacy lockfiles/node_modules (in-repo trust — not a new package); post-install `npm ls esbuild` version-alignment check against the legacy pin |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Single install: esbuild ^0.28.1, version-aligned with existing repo lockfiles; no other packages touched |
</threat_model>

<verification>
- `ls plugins/` → narrative-tool only
- `node plugins/narrative-tool/esbuild.config.mjs` → builds with exit 0
- `node --test tests/*.test.js` → fail 0 (full suite, ~234 pre-merge + new tests)
- `git status --porcelain tests/golden` → identical to phase-start baseline (goldens byte-identical)
- `node --test tests/engine-purity.test.js` → pass (engine boundary intact in final layout)
- `grep -rn "require(.*dialogue-export\|require(.*narrative-project\|require(.*flow-tools" tests/` → zero matches (require-path scoped — the migration string literal in merge-smoke.test.js is intentional)
</verification>

<success_criteria>
- [ ] plugins/ contains exactly plugins/narrative-tool/
- [ ] plugins/narrative-tool/main.js builds with all 10 narrative-tool: command IDs, no legacy ids
- [ ] Full test suite green (fail 0) from the clean single-plugin state
- [ ] 9 golden files byte-identical (no new golden churn vs. phase start)
- [ ] Engine purity guard green in final layout
- [ ] Phase 5 success criteria 1-5 from ROADMAP.md are satisfiable: single plugin with all commands, Quest creation (BUG-01/07), Flow templates + reverse nav (BUG-04/05), Export Path honored (BUG-02/03), data-nt-type visual distinction (BUG-06)
</success_criteria>

<output>
Create `.planning/phases/05-plugin-merge-bug-fixes/05-05-SUMMARY.md` when done
</output>
